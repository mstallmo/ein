package ein:plugin;

world plugin {
    import host;

    export tool: interface {
        resource tool {
            constructor();
            name: func() -> string;
            schema: func() -> string;
            enable-chunk-sender: func() -> bool;
            call: func(id: string, args: string) -> result<string, string>;
        }
    }
}
